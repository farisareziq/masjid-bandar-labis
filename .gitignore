# Dependencies (tiada dependency luar sepatutnya)
node_modules/

# Output build (dijana semula oleh CI / `node src/build.js`)
dist/

# Fail sistem operasi & editor
.DS_Store
Thumbs.db
*.log
.vscode/
.idea/

# Fail sensitif / rahsia — JANGAN commit ke GitHub
*.key
*.pem
.env
.env.*
secrets.*

# Video aerial terlalu besar untuk git (646 MB, melebihi had 100 MB GitHub)
# Cadangan: muat naik ke YouTube dan embed dalam halaman Aktiviti.
images/Video Aerial Masjid/
docs/images/Video Aerial Masjid/

# Sandaran gambar asal daripada scripts/optimize-images.ps1
backup-images/

# Token TikTok (hasil OAuth) & fail sementara muat turun
scripts/tiktok/tokens.json
scripts/tiktok/tmp/

# Contoh konfigurasi ikut di-commit (bukan rahsia)
!.env.example

# Fail kerja/pelan — jangan biar auto-deploy (docs/) sentuh atau padam
docs/PELAN-*.md
